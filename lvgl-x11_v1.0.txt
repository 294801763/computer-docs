安装依赖
--------------
apt-get install check libevdev-dev libudev-dev libsdl2-dev libxkbcommon-dev libinput-dev libinput-tools x11-xserver-utils libgtk-3-dev

更新SDL
--------------

更新libinput 
--------------
meson setup build --prefix=/usr
ninja -C build install
or 
meson setup build -Denable-debug-gui=false --prefix=/usr
ninja -C build install

TTY自动登录
--------------
$>nano /etc/systemd/system/getty.target.wants/getty@tty1.service
--
...
ExecStart=-/sbin/agetty -o '-p -f -- \\u' --noclear --autologin root %I $TERM
...

LVGL
--------------
$>apt-get install libx11-dev libxext-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libx11-6 xserver-xorg-core xserver-xorg build-essential linux-headers-`uname -r` tree net-tools keepalived
$>nano /usr/bin/x && chmod 777 /usr/bin/x
--
#!/bin/bash 

export DISPLAY=:0.0
export XAUTHORITY=/root/.Xauthority

/usr/bin/Xorg :0.0 -verbose vt7 &

开发专用命令
--------------
$>libinput list-devices
$>setxkbmap -query

LibINPUT排查命令
--------------
libinput --version
pkg-config --modversion libinput

LIBINPUT_DEBUG=1 libinput debug-events                     #观察输出并修正内容" /usr/share/libinput "中的内容即可
libinput quirks list /dev/input/event0
libinput quirks list /dev/input/event0 --verbose

libinput quirks list /dev/input/event7
libinput quirks list /dev/input/event7 --verbose

libinput quirks validate --verbose --data-dir /usr/share/libinput
libinput quirks list --data-dir /usr/share/libinput /dev/input/event0

nano  /etc/libinput/local-overrides.quirks
--
[Serial Keyboards]
MatchUdevType=keyboard
MatchBus=serial
AttrKeyboardIntegration=internal

nano /usr/share/libinput/30-vendor-huion.quirks
--
# Do not edit this file, it will be overwritten on update

# HUION PenTablet device. Some of these devices send a BTN_TOOL_PEN event
# with value 1 on the first event received by the device but never send the
# matching BTN_TOOL_PEN value 0 event. The device appears as if it was
# permanently in proximity.
#
# HUION re-uses USB IDs for its devices, not every HUION tablet is
# affected by this bug, libinput will auto-disable this feature
[HUION PenTablet]
MatchUdevType=tablet
MatchBus=usb
MatchVendor=0x256C
#ModelTabletNoProximityOut=1

更新meson
----
python3 -m pip install --upgrade meson

修改GRUB
----
$>nano /etc/default/grub
--
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash video=1920x1080"

$>update-grub
$>nano /etc/X11/xorg.conf
--
Section "Screen"
    Identifier     "Default Screen"
    Device         "Videocard0"
    Monitor        "Monitor0"
    DefaultDepth    24
    Option         "ForceCompositionPipeline" "disable"
    SubSection "Display"
        Depth       24
        Modes       "1920x1080"
    EndSubSection
EndSection

